# AI 产品知识助手 —— 升级规划（Upgrade Plan）

> 目标：从「一个能跑的 Streamlit Demo + 一个没接上电的前后端外壳」
> 升级为「前后端分离、可公网部署、带评测闭环的 AI 产品工作台」。
>
> 面向场景：AI 产品岗求职作品集。技术选型的第一评判标准是
> **「面试时能不能讲成一个有取舍、有数据支撑的产品决策」**，而不是技术新潮度。

---

## 0. 现状体检（基于代码实读，非推测）

| 模块 | 文件 | 行数 | 实际状态 | 关键问题 |
|---|---|---|---|---|
| Streamlit 主应用 | `app.py` | 481 | 登录 / BYOK / 上传 / 问答 / 导出全通 | **当前唯一真正能跑的完整闭环** |
| FastAPI 后端 | `backend/main.py` | 32 | 仅 SSE 骨架 | **导入即崩**：`from core.rag import process_query`，该函数在 `core/rag.py` 中不存在 |
| Next.js 前端 | `frontend/src/app/page.js` | 144 | 单文件聊天页 | **文件语法不完整**（结尾缺 `)` 与 `}`，`npm run dev` 无法编译）；文档树是 3 个写死的 div；SSE 用 `split('data: ')` 解析不可靠；硬编码 `http://localhost:8000` |
| RAG 核心 | `core/rag.py` | 334 | 关键词 + 向量双检索，可自动降级 | 无流式；检索为全内存 O(n) 扫描；**`save_kb()` 每次上传覆盖整个 `kb.json`** → 每个用户实际只能拥有「一个知识库」 |
| 存储 | `core/storage.py` | 192 | `users` / `conversations` / `messages` | 无 `documents` 表、无 `chunks` 表、无 `feedback` 表 |
| 语料 | `producttext/*.md` | 2 篇 | 面试知识库 + 电商客服 PRD | 可直接作为评测金标集的出题来源 |

### 结论

现状**不是「空壳」，而是「一台能跑的旧车 + 一副还没接线的新车壳」**。

升级的本质是：**把 `app.py` 里已经验证过的业务逻辑抽成 API 层，让 Next.js 真正接管**，
而不是推倒重写。这决定了下面所有阶段都是「改造」而非「新建」，风险可控。

### 最致命的一处（决定了后续所有卖点能否成立）

`core/rag.py:132-138` 的覆盖式写入：

```python
kb_file = user_dir / "kb.json"
kb_file.write_text(json.dumps({"mode": mode, "chunks": chunks_meta}, ...))
```

只要不修这里，「左侧文档树」「多文档交叉对比」「按文档筛选检索」
就永远只能是假的 UI。**P1 是整个规划的地基，不能跳过。**

---

## 1. 技术路线的取舍（面试时要能讲出「为什么不做」）

网上说「RAG 在 2026 落伍了」，准确的说法是：
**Naive RAG（固定分块 + 单一向量余弦 + 直接塞进 Prompt）落伍了**，
而不是检索增强这件事本身落伍了。长上下文越普及，
「检索什么、给多少、凭什么相信」反而越是产品问题。

### 明确不做：Graph RAG

不是做不了，是**性价比不成立**，且这个判断本身就是加分项：

1. 语料是 PRD / 竞品报告，实体关系稀疏，建图收益低
2. 图谱构建 + 维护成本高（抽取实体关系需大量 LLM 调用，成本随文档量线性上升）
3. 面试官大概率会追问「你为什么用图？成本算过吗？增量更新怎么做？」——
   答不上来反而是减分

> **面试话术**：「我评估过 Graph RAG，但我的语料是需求文档，
> 实体关系稀疏，建图的 LLM 抽取成本大约是普通入库的 8-10 倍，
> 而我的核心痛点是『引用不准 + 幻觉』，用父子分块 + 混合检索 + 拒答阈值
> 能以 1/10 的成本解决 80% 的问题。所以我把预算花在了评测体系上。」

### 明确要做（按「性价比」而非「技术炫酷度」排序）

| 优先级 | 能力 | 工作量 | 面试价值 | 理由 |
|---|---|---|---|---|
| ★★★★★ | **评测闭环**（金标集 + 指标看板 + 点踩归因） | 小 | **极高** | 90% 候选人没有；能把所有技术改动变成「数字提升」 |
| ★★★★★ | **父子分块**（子块检索 / 父块注入） | 小 | 高 | 改动集中在 `chunk_text`，效果最直观 |
| ★★★★ | **混合检索 + RRF 融合** | 中 | 高 | 两套检索器**代码里已经有了**，只差一个 ~30 行的 RRF |
| ★★★★ | **拒答阈值 + 幻觉护栏** | 极小 | 中高 | 成本近乎为零，体现风控产品意识 |
| ★★ | Query Router（意图路由） | 中 | 中 | 锦上添花，做不完不影响主线叙事 |
| ★ | Agentic 多步检索 | 大 | 中 | 时间充裕再考虑，容易做成不稳定的花架子 |

---

## 2. 目标架构

```
┌──────────────────────────────────────────────────┐
│  Next.js (Vercel)                                │
│  三栏：文档树 │ 流式对话 │ 引用溯源抽屉           │
│  /dashboard：评测指标看板                        │
└───────────────┬──────────────────────────────────┘
                │ HTTPS + SSE
┌───────────────▼──────────────────────────────────┐
│  FastAPI (Railway / Render — 需长连接，非 Serverless) │
│  /auth  /documents  /chat/stream  /feedback      │
├──────────────────────────────────────────────────┤
│  core/  ← 复用现有逻辑，仅补流式 + 混合检索        │
├──────────────────────────────────────────────────┤
│  Repository 层（接口）                            │
│    ├─ SQLite   （本地开发）                       │
│    └─ Postgres + pgvector（Supabase / 线上）      │
└──────────────────────────────────────────────────┘

Streamlit (app.py) ── 保留，转型为「内部调试台 / 评测工作台」
```

### 关于 Streamlit 的去留

**保留，不要删。** 定位改为「内部调试与评测工作台」，Next.js 作为对外产品面。

这本身就是一个可讲的产品决策（内部工具追求迭代速度，对外产品追求体验），
而不是「有两套没清干净的代码」。README 中需明确写出这个定位，否则会被误读。

---

## 3. 分阶段实施

### P0 · 打通血脉：让现有骨架真的能跑

> 判定标准：在 Next.js 里输入问题 → 字逐个吐出来 → 底部出现真实引用来源。
> 这一步不加任何新功能，只把「断掉的线」接上。

| # | 任务 | 涉及文件 |
|---|---|---|
| 0.1 | 补全 `page.js` 结尾缺失的 `)` 和 `}`，确认 `npm run dev` 能编译 | `frontend/src/app/page.js` |
| 0.2 | 在 `core/rag.py` 新增 `stream_answer()` 生成器（`chat.completions.create(stream=True)`），复用现有 `retrieve()` | `core/rag.py` |
| 0.3 | 删除 `backend/main.py` 中不存在的 `process_query` 导入，改接 `stream_answer` | `backend/main.py` |
| 0.4 | SSE 改为规范事件格式：`event: sources` → `event: token`(N次) → `event: done`，携带耗时与 token 统计 | `backend/main.py` |
| 0.5 | 前端改用逐行 buffer 解析 SSE（不再用 `split('data: ')`，该写法遇到内容含 `data: ` 或分片截断即错乱） | `frontend/src/app/page.js` |
| 0.6 | 抽出 `src/lib/api.js`，用 `NEXT_PUBLIC_API_BASE` 替代硬编码 localhost，补 `.env.example` | `frontend/src/lib/api.js` |
| 0.7 | 后端 CORS 白名单从环境变量读取，为部署做准备 | `backend/main.py` |

**产出**：一条真实可用的端到端流式链路。
**风险**：低。不动数据结构。

---

### P1 · 数据层重构：让「多文档」从假变真

> 这是地基。跳过它，P2/P3 的展示效果都会打对折。

| # | 任务 | 说明 |
|---|---|---|
| 1.1 | `storage.py` 新增 `documents` 表 | `id / user_id / filename / status(pending\|parsing\|ready\|failed) / chunk_count / created_at` |
| 1.2 | `storage.py` 新增 `chunks` 表 | `id / doc_id / parent_id / text / embedding / seq` |
| 1.3 | **废弃 `kb.json` 覆盖式写入**，改为按文档增量入库 | 修 `core/rag.py:100-139`，这是整个 P1 的核心 |
| 1.4 | 写一次性迁移脚本，把已有 `kb.json` 导入新表 | `scripts/migrate_kb.py`，避免旧数据丢失 |
| 1.5 | 上传接口改异步：返回 `task_id`，后台协程解析，前端轮询状态 | `backend/main.py` + `documents` 表的 `status` 字段 |
| 1.6 | 检索支持 `doc_ids` 过滤参数 → 多文档交叉对比的技术基础 | `core/rag.py: retrieve()` |
| 1.7 | 前端三栏布局落地：左文档树（真实数据 + 上传进度 + 解析状态）、中对话、右引用抽屉 | `frontend/src/app/` |

**产出**：左侧文档树显示真实文档；可勾选文档范围提问；上传有进度与状态反馈。
**风险**：中。涉及数据结构变更，务必先写 1.4 的迁移脚本。

---

### P2 · 检索升级：把「技术答案」做出来

> **硬性要求：每完成一项，立刻跑一次 P3 的评测脚本，记录 before/after 数字。**
> 不记录数字的检索优化 = 白做，因为面试时无法证明。

| # | 任务 | 实现要点 |
|---|---|---|
| 2.1 | **父子分块** | `chunk_text` 拆为 `split_parents(~1500字)` → `split_children(~300字)`；检索命中子块，注入父块全文 |
| 2.2 | **BM25** | 纯 Python 实现（约 40 行），复用已有的 `_tokenize()` 中文二元切分，**不引入重依赖** |
| 2.3 | **RRF 融合** | `score = Σ 1/(k + rank_i)`，k=60；把已有的 `_keyword_retrieve` 与 `_vector_retrieve` 结果融合 |
| 2.4 | **拒答阈值** | 融合分低于阈值时，返回「文档中未提及此信息」并提示可补充哪类文档，而非硬编答案 |
| 2.5 | 场景化 Prompt 模板 | 「PRD 风险审查」「竞品差距分析」「面试追问提炼」三个快捷入口；本质是预设 Prompt，成本低但产品感强 |
| 2.6 | *(可选)* Query Router | 事实型查询走 BM25，概念型走向量，对比型走多文档；时间不够可砍 |

**产出**：三档可切换的检索策略（naive / 混合 / 混合+父子），供 P3 做横向对比。
**风险**：中低。建议用配置开关控制策略，便于 A/B 与回滚。

---

### P3 · 评测闭环：真正的差异化

> **这一阶段的投入产出比最高。** 工作量小于 P2，但面试价值高于 P2。

| # | 任务 | 说明 |
|---|---|---|
| 3.1 | `feedback` 表 + 每条回答的 👍/👎 | 点踩弹归因选项：内容幻觉 / 切片不全 / 答非所问 / 引用错误 |
| 3.2 | **金标集** `evals/goldset.json` | 基于 `producttext/` 两篇文档**手写 50 条**问答对，标注应命中的 chunk。手写，不要让 LLM 生成——自造自测无说服力 |
| 3.3 | `evals/run_eval.py` | 输出：命中率@4、Faithfulness、答案相关度、TTFT、平均检索耗时 |
| 3.4 | `/dashboard` 指标看板页 | 展示 naive / 混合 / 混合+父子 三个版本的横向对比表 |
| 3.5 | 埋点：TTFT、检索耗时、token 消耗落库 | 看板数据来源，同时体现数据驱动意识 |

**产出**：一张能直接放进简历和作品集的指标对比表。

> **面试表达效果**：别人说「我调了 Prompt，chunk 用了 500」，
> 你说「我用 50 条金标集做基准，父子分块把命中率@4 从 X% 提到 Y%，
> 混合检索再提 Z%，Faithfulness 从 A% 到 B%，TTFT 控制在 600ms 内。
> 代价是入库耗时增加 40%，我认为对低频写入、高频查询的场景是划算的。」

---

### P4 · 部署上线：前后端都能公网访问

| 层 | 方案 | 关键注意事项 |
|---|---|---|
| 前端 | **Vercel** | Next.js 原生支持，绑 GitHub 自动部署 |
| 后端 | **Railway / Render** | **不要用 Vercel Serverless 跑 FastAPI**——SSE 需要长连接，Serverless 会超时截断 |
| 数据 | **Supabase Postgres + pgvector** | SQLite 在容器中重启即丢；免费额度足够作品集使用 |
| 密钥 | 平台环境变量 | 延续现有 BYOK 设计：**用户 Key 绝不落库**，仅在请求生命周期内使用 |

| # | 任务 |
|---|---|
| 4.1 | `storage.py` 抽出 Repository 接口，SQLite / Postgres 双实现，本地开发无需连云 |
| 4.2 | 后端 Dockerfile + 健康检查端点 `/health` |
| 4.3 | 环境变量梳理：`.env.example`（前后端各一份） |
| 4.4 | README 补部署说明 + 架构图 + 在线体验入口 + demo 账号 |

---

## 4. 交付物清单（作品集要的不只是代码）

- [ ] 在线可访问的前端地址 + 可用的 demo 账号
- [ ] `docs/PRD.md` —— 更新至 V3.0，对齐本规划
- [ ] `docs/DESIGN.md` —— **架构与权衡分析**（重点写「为什么不做 Graph RAG」「为什么保留 Streamlit」「为什么后端不上 Serverless」）
- [ ] `docs/EVAL_REPORT.md` —— 评测指标报告，含三版横向对比表
- [ ] `evals/goldset.json` —— 50 条手写金标集
- [ ] README —— 架构图 + 快速开始 + 部署指南

---

## 5. 推荐执行顺序（时间有限时）

```
P0 → P1 → P3（评测）→ P2（检索优化）→ P4（部署）
```

**为什么 P3 插到 P2 前面**：

1. 评测脚本先就位，P2 的每一次检索改动才有 before/after 数字可记录——
   否则做完优化再补评测，等于要把所有实验重跑一遍
2. 评测看板的工作量小于检索优化，但面试差异化更大
3. 万一时间不够砍掉 P2，「有评测体系但检索朴素」
   仍然是一个完整故事；反过来「检索花哨但无数据支撑」则讲不通

### 各阶段最小可交付形态

| 完成到 | 能对外讲的故事 |
|---|---|
| P0 | 「前后端分离的流式问答应用」——及格线 |
| P0+P1 | 「支持多文档管理与引用溯源的知识工作台」——像个产品了 |
| P0+P1+P3 | **「带评测闭环的 AI 产品工作台」——最小可信版本，建议至少做到这里** |
| 全部 | 「有技术纵深 + 数据支撑 + 完整部署的成熟原型」 |

---

## 6. 风险与对策

| 风险 | 影响 | 对策 |
|---|---|---|
| P1 数据迁移丢数据 | 高 | 先写 `scripts/migrate_kb.py` 并备份 `data/`，迁移前后对比 chunk 总数 |
| 范围蔓延，什么都想做 | 高 | 严守优先级；Query Router / Agentic 检索默认不做，只在全部完成后考虑 |
| 评测集偷懒用 LLM 生成 | 中 | 必须手写。自造自测的指标在面试中一问就穿 |
| 免费部署平台休眠导致首次访问慢 | 中 | README 注明冷启动约 30s，或加定时保活 |
| Streamlit 与 Next.js 逻辑双份维护 | 中 | 业务逻辑只留在 `core/`，两端都只做展示层，不复制逻辑 |

---

## 7. 下一步

建议从 **P0.1 + P0.2** 开始——补全 `page.js` 的语法、给 `core/rag.py` 加上
`stream_answer()`，当天就能在浏览器里看到字真的在流出来。
先有正反馈，再谈重构。
